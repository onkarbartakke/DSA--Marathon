#include<bits/stdc++.h>

using namespace std;

int main()
{
   
    int t;

    cin>>t;

    while(t--)
    {
        int n;
        
        vector<int>a(n);

        for(int i = 0 ; i < n ; i++)
        cin>>a[i];

        sort(a.begin() , a.end());

        if(n%2 == 1)
        {
            cout<<"No\n";
        }
        else
        {
            vector<int>temp(n);

            for(int i = 0 ; i < n ; i++)
            {
                
            }
        }
    }
}